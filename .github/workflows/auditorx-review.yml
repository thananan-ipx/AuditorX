name: AuditorX Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  audit:
    runs-on: ubuntu-latest
    env:
      AUDITORX_API_URL: https://auditor-x.vercel.app/api/reviews
      PR_NUMBER: ${{ github.event.pull_request.number }}
      PR_TITLE: ${{ github.event.pull_request.title }}
      PR_URL: ${{ github.event.pull_request.html_url }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4

      - name: Build PR diff payload
        run: |
          gh pr diff "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" > pr.diff
          jq -n \
            --arg repoFullName "$GITHUB_REPOSITORY" \
            --argjson pullNumber "$PR_NUMBER" \
            --arg pullTitle "$PR_TITLE" \
            --arg pullUrl "$PR_URL" \
            --rawfile diffContent pr.diff \
            '{ repoFullName: $repoFullName, pullNumber: $pullNumber, pullTitle: $pullTitle, pullUrl: $pullUrl, diffContent: $diffContent }' \
            > payload.json

      - name: Ask AuditorX for a review
        run: |
          curl -sS -X POST "$AUDITORX_API_URL" \
            -H "Authorization: Bearer ${{ secrets.AUDITORX_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            --data @payload.json \
            > auditorx-response.json
          jq -r '.comment' auditorx-response.json > auditorx-comment.md

      - name: Post AuditorX comment
        if: github.event.pull_request.head.repo.full_name == github.repository
        run: gh pr comment "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --body-file auditorx-comment.md

      - name: Skip comment for forked pull requests
        if: github.event.pull_request.head.repo.full_name != github.repository
        run: |
          echo "Skipping PR comment because this pull request comes from a fork."
          echo "GitHub does not grant the default GITHUB_TOKEN write access for forked pull_request workflows."
